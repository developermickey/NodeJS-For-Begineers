<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav flex gap-5 mb-10">
            <a class="text-zinc-500" href="/">Back To Home</a>
            <a class="text-zinc-500" href="/read">Read All Users</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-5">All User</h1>
        <div class="users flex flex-wrap gap-3">
            <% if(users.length> 0) {%>
                <% users.forEach((user)=> {%>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg ">
                        <div class="w-full h-52 bg-zinc-700 rounded-lg overflow-hidden">
                            <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter">
                            <%= user.name %>
                        </h3>
                        <h5 class="text-zinc-500">
                            <%= user.email %>
                        </h5>
                        <div class="w-full flex gap-3 mt-10 items-center justify-between">
                            <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>

                    <% }) %>
                        <% }else { %>
                            <h4>No User Found</h4>
                            <% } %>


        </div>
    </div>
</body>

</html>