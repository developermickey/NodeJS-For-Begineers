<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-between w-full border-b-2 mb-5">
            <h3 class="text-2xl">Logo</h3>
            <div>
                <a class="rounded-md px-3 py-2 mb-5 inline-block" href="/">Home</a>
                <a class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block" href="/logout">Logout</a>
            </div>
        </div>
        <h2 class="text-3xl tracking mb-5">Profile</h2>

        <div class="flex gap-3">
            <div class="w-10 h-10 rounded-md">
                <img class="w-full h-full object-cover rounded-md" src="/images/uploads/<%= user.profileimg %>" alt="">
            </div>
            <h3 class="text-3xl mb-5">
                <span class="font-light">Hi, </span>
                <%= user.name %>
            </h3>
        </div>

        <h5 class="mt-5 mb-5">You Can Create a Posyt</h5>

        <form action="/post" method="post">
            <textarea placeholder="What on your mind ?"
                class="block w-1/3 p-3  outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"
                name="content"></textarea>
            <br>
            <input class="w-40 block px-5 py-2 bg-blue-500 rounded-md" type="submit" value="Post">
        </form>

        <div class="posts mt-5">
            <h3 class="text-zinc-400">Your Posts</h3>

            <div class="postcontainer mt-5 flex flex-wrap gap-5">

                <% user.posts.reverse().forEach((post)=> { %>
                    <div class="post w-1/4 p-4 mb-4 border-[1px] rounded-md border-zinc-600 bg-zinc-800 ">
                        <h4 class="text-blue-400 mb-3">@ <%= user.username %>
                        </h4>
                        <p class="text-sm ">
                            <%= post.content %>
                        </p>
                        <small class="mt-4 inline-block">
                            <%= post.likes.length %> likes
                        </small>
                        <div class="btns flex mt-5 justify-between mt-4">
                            <a class="text-blue-400" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>

                        </div>
                    </div>

                    <% }) %>

            </div>

        </div>

    </div>

</body>

</html>