<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="w-full block px-5 py-2 bg-zinc-700 rounded outline-none text-white" type="text"
                    placeholder="Task" name="title">
                <textarea class="w-full block px-5 mt-2 py-2 resize-none bg-zinc-700 rounded outline-none text-white"
                    placeholder="Write Task Message" name="details"></textarea>
                <input type="submit" class="mt-2 bg-blue-600 px-5 py-2 rounded text-white" value=" Task Create">
            </form>
        </div>
        <div class="tasks flex gap-3 p-10 text-white">
            <% if(files.length> 0) {%>
                <% files.forEach((val)=> { %>
                    <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded">
                        <h1 class="text-white text-3xl tracking-tighter ">
                            <%= val %>
                        </h1>
                        <a href="/file/<%= val %>" class="text-blue-500 inline-block mt-5">Read More</a>
                    </div>

                    <% }) %>

                        <% }else { %>

                            <h3 class="text-zinc-600">No Task Yet!!!</h3>
                            <% } %>
                                <div class="task w-72 hidden px-3 py-4 bg-zinc-800 rounded">
                                    <h1 class="text-white text-3xl tracking-tighter ">Some Title</h1>
                                    <a href="" class="text-blue-500 inline-block mt-5">Read More</a>
                                </div>
                                <div class="task w-72 hidden px-3 py-4 bg-zinc-800 rounded">
                                    <h1 class="text-white text-3xl tracking-tighter ">Some Title</h1>
                                    <a href="" class="text-blue-500 inline-block mt-5">Read More</a>
                                </div>
                                <div class="task w-72 hidden px-3 py-4 bg-zinc-800 rounded">
                                    <h1 class="text-white text-3xl tracking-tighter ">Some Title</h1>
                                    <a href="" class="text-blue-500 inline-block mt-5">Read More</a>
                                </div>

        </div>
    </div>

</body>

</html>